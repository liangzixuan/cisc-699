services:
  safeexec-api:
    image: python:3.11-slim
    working_dir: /workspace
    volumes:
      - ..:/workspace:ro
    environment:
      PYTHONPATH: /workspace/src
    command: python -m safeexec.api.server --host 0.0.0.0 --port 8080 --backend local
    ports:
      - "8080:8080"
